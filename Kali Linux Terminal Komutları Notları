# kali Linux Notları
whoami = şu anda hangi kullanıcıyla oturum açtığını gösterir
pwd şu anki çalıştığın dizini gösterir benimki winden
ls dosyaları gösterir
ls -la gizli dosyaları (. ile başlayan dosyaları gösterir)
rm dosyayı siler. rm = deneme/test_moved.txt yazarsam deneme içinde ki test_moved.txt adlı dosyayı silecek
chown sahipliği değiştirir.
chmod = dosya izinlerini değiştirir.
chgrp = dosya gruplarını değiştirir.
sudo whoami root varmı diye kontrol eder.
sudo = geçici root yetkisi yani normalde root yetkimiz yoksa sudo yazarak root yetkisi alabilir o satır için
chmod 600 = sadece owner yazabilir ve okuyabilir
chmod 644 sadece owner yazabilir diğerleri okuyabilir.
4=read
2=write
1=execute
600 yazarsak sadece owner yazabilir okuyabilir ve çalıştırabilir.
755 yazarsak owner yazabilir okuyabilir ve çalıştırabilir, diğerleri okuyabilir ve çalıştırabilir.
cat dosyaları gösterir, dosyalar oluşturabilir ve içine yazı yazdırabilir.
more komutu dosyadaki fazla yazıları sayfa sayfa göstermeye yarar.
head mesaj.txt = mesaj.txt de ki yazıları gösterir
head -n 3 mesaj.txt = mesaj.txt yazılarının ilk 3 satırını gösterir.
tail -n 1 mesaj.txt = mesaj.txt yazılarının son 1 satırını gösterir.
grep 'mustafa' mesaj.txt = mesaj.txt nin içinde ki yazıların içinde mustafa var mı diye kontrol eder varsa getirir.
KULLANICI EKLEME SİLME VE ŞİFRE GÜNCELLEME----
sudo su 
aduser mustafa kullanıcı ekler
deluser mustafa kullanıcıyı siler ama dosyalardan silmez dosyaları kalır
deluser mustafa --remove-home mustafa = mustafanın dosyasını da siler.
passwd mustafa = şifreyi değiştirir.
passwd -l mustafa = girişi kitler
passwd -u mustafa = girişi açar
-l = lock
-u = unlock
-----------------------------------------------
addgroup bilgiislem = grup oluşturur.
delgroup bilgiislem = grubu siler.
getent group = grupları ve kimlerin üye olduğunu gösterir.
usermod -a -G bilgiislem ali = aliyi bilgiislem grubuna üye yapar.
SERVİS YÖNETİMİ -------------------------------------------------
service --status-all = tüm servisleri gösterir.
systemctl status procps = procps servisinin çalışıp çalışmadığını gösterir
service procps start = servisi başlatır.
service procps restart = servisi yeniden başlatır.
service procps stop = servisi durdurur.
------------------------------------------------------------------
cp kopyalamak için.
mv taşımak veya dosyanın ismini değiştirmek için.
cp nin kullanımı = cp kaynak(dosya neredeyse) taşımak istenen yer.
bin dosyasına atılan her dosyayı terminalde sadece ismini yazarak çalıştırabiliriz.
echo $BASH_VERSION = kullandığın bashin versiyonunu gösterir.
echo $HOSTNAME = host adını gösterir.
echo $HOME = kullanıcnın home klasörünü gösterir.
echo $PWD = Şu anda hangi dizinde olduğumuzu gösterir.
echo $LANG = bilgisarın dilini gösterir.
echo $USER = Oturumu açan kişiyi gösrerir.

